name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Web UI
        working-directory: vendor/web-ui
        run: |
          npm ci
          npm run build -- --base=./

      - name: Debug environment
        shell: bash
        run: |
          echo "Runner OS: $RUNNER_OS"
          echo "Git:"
          git --version
          echo "Repo HEAD:"
          git rev-parse HEAD
          echo "Submodules:"
          git submodule status --recursive || true
          echo "JUCE submodule HEAD (if present):"
          if [ -d vendor/JUCE/.git ] || [ -f vendor/JUCE/.git ]; then
            git -C vendor/JUCE rev-parse HEAD
            git -C vendor/JUCE remote -v || true
          else
            echo "vendor/JUCE is missing or not a git checkout"
            ls -la vendor || true
            ls -la vendor/JUCE || true
          fi
          echo "CMake:"
          cmake --version
          echo "Compiler:"
          if [ "$RUNNER_OS" = "Windows" ]; then
            cl 2>&1 | head -n 20 || true
          else
            c++ --version || true
            clang++ --version || true
            g++ --version || true
          fi

      - name: Install WebView2 SDK (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # JUCE's FindWebView2.cmake searches for a directory whose name matches
          # *Microsoft.Web.WebView2* inside JUCE_WEBVIEW2_PACKAGE_LOCATION.
          # We install via nuget.exe with -OutputDirectory so the package lands
          # directly in $nugetDir/Microsoft.Web.WebView2.<version>/.
          $nugetDir = "$env:USERPROFILE\AppData\Local\PackageManagement\NuGet\Packages"
          New-Item -ItemType Directory -Force -Path $nugetDir | Out-Null

          nuget install Microsoft.Web.WebView2 `
            -OutputDirectory $nugetDir `
            -NonInteractive

          # Verify the package directory was created and report it.
          $packageDir = Get-ChildItem -Path $nugetDir -Directory -Filter "Microsoft.Web.WebView2*" |
            Sort-Object LastWriteTime -Descending |
            Select-Object -First 1

          if (-not $packageDir) {
            Write-Error "Microsoft.Web.WebView2 package directory not found under $nugetDir after nuget install."
            exit 1
          }

          Write-Host "WebView2 package installed at: $($packageDir.FullName)"
          Write-Host "WebView2 include check: $(Test-Path (Join-Path $packageDir.FullName 'build\native\include\WebView2.h'))"

          # Export the parent directory so CMake's FindWebView2.cmake can glob for
          # the *Microsoft.Web.WebView2* subdirectory automatically.
          echo "JUCE_WEBVIEW2_PACKAGE_LOCATION=$nugetDir" >> $env:GITHUB_ENV

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          # JUCE WebBrowser on Linux expects WebKitGTK 4.1 (fallback to 4.0)
          if apt-cache show libwebkit2gtk-4.1-dev >/dev/null 2>&1; then
            webkit_package="libwebkit2gtk-4.1-dev"
          elif apt-cache show libwebkit2gtk-4.0-dev >/dev/null 2>&1; then
            webkit_package="libwebkit2gtk-4.0-dev"
          else
            echo "No supported WebKitGTK dev package found (need 4.1 or 4.0)"
            exit 1
          fi

          sudo apt-get install -y \
            libasound2-dev \
            libx11-dev \
            libxcomposite-dev \
            libxcursor-dev \
            libxext-dev \
            libxinerama-dev \
            libxrandr-dev \
            libxrender-dev \
            libgl1-mesa-dev \
            libgtk-3-dev \
            libcurl4-openssl-dev \
            "$webkit_package"

      - name: Configure
        run: cmake --preset release -DWEBUI_DIST_PATH=${{ github.workspace }}/vendor/web-ui/dist

      - name: Build on non-Windows
        if: runner.os != 'Windows'
        run: cmake --build --preset release -- -j 2

      - name: Build on Windows
        if: runner.os == 'Windows'
        run: cmake --build --preset release

      - name: Verify packaged WebUI (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $webuiIndex = Get-ChildItem -Path out/build/release -Recurse -File -Filter index.html |
            Where-Object { $_.FullName -match '[\\/]WebUI[\\/]index\.html$' } |
            Select-Object -First 1

          if (-not $webuiIndex) {
            Write-Error "WebUI/index.html not found under out/build/release"
            exit 1
          }

          Write-Host "Found packaged WebUI at: $($webuiIndex.FullName)"

      - name: Verify packaged WebUI (non-Windows)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          webui_index="$(find out/build/release -type f -path '*/WebUI/index.html' | head -n 1)"
          if [[ -z "$webui_index" ]]; then
            echo "WebUI/index.html not found under out/build/release" >&2
            exit 1
          fi

          echo "Found packaged WebUI at: $webui_index"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: out/build/release
